spring:
  application:
    name: wealth-link-platform
  datasource:
    url: jdbc:postgresql://postgres.coolifyprojects.techpulseitservices.com:5433/wealth_link
    username: weatlh
    password: Weatlh@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate   # Schema is validated against Flyway migrations
    properties:
      hibernate:
        format_sql: true
        default_schema: public
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug